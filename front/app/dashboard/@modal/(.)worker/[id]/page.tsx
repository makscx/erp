import WorkerModal from "@/app/dashboard/_components/WorkerTasksModal";
import type { Worker } from "@/types/worker";

export default function WorkerPage({ worker }: { worker: Worker }) {
    return (
        <div>
            asd

        </div>
    );
}